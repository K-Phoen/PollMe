{% extends "base.html.twig" %}

{% block content %}
<div class="container">
    <div class="span7 offset2">
        {% include "flashes.html.twig" %}

        <ul class="media-list">
            {% for survey in surveys %}
                {% include "surveys/survey.html.twig" %}
            {% else %}
                <li class="media well">Aucun sondage ne correspond à la recherche "{{ search }}".</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}
