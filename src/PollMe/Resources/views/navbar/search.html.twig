<form class="form-search navbar-form pull-left" method="post" action="{{ route('surveys_search') }}">
  <div class="input-append">
    <input name="keyword" type="text" class="span2 search-query" value="{{ request.request.get('keyword', '') }}" />
    <button type="submit" class="btn">Rechercher</button>
  </div>
</form>
